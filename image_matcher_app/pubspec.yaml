name: image_matcher_app
description: 根据图片匹配本地图库并返回正确名称
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.4
  file_picker: ^5.3.3
  archive: ^3.4.0
  excel: ^2.0.0
  image: ^4.1.3
  shared_preferences: ^2.2.2
  permission_handler: ^10.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
